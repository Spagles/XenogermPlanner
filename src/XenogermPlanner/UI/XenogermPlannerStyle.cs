namespace XenogermPlanner.UI
{
    internal static class XenogermPlannerStyle
    {
        internal static class Metrics
        {
            internal const float GeneIconSize = 22f;
            internal const float GeneIconGap = 6f;
            internal const float GenepackTargetSize = 26f;
            internal const float GenepackTargetGap = 3f;

            internal const float TooltipWideWidth = 560f;
            internal const float TooltipMinimumGroupColumnWidth = 220f;
            internal const float TooltipGroupGap = 12f;
            internal const float TooltipGroupHeaderHeight = 22f;
            internal const float TooltipGroupVerticalGap = 6f;
            internal const float TooltipRowHeight = 26f;
        }
    }
}